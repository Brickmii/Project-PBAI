# Evaluator module - Golden Loop learning,
# responsible for gating values observed through FFT,
# gates learned values to v_kernel,
# weighs values learned to values known,
# updates values per environment,
# updates values per system